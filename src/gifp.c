#include "stdio.h"
#include "stdlib.h"
#include "util.h"

int main(int argc, char **argv) {
    if (argc != 3) return 0;

    int n = atoi(argv[1]);
    int k = atoi(argv[2]);

    // printf("%i\n", is_prime(n, k));
}