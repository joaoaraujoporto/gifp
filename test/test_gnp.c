#include "../src/gnp.h"
#include "../src/util.h"

void test_gnp() {
    /**
     * 0 is not really expected in this block
     */
    // assert_equal(0, gnp(0, 1));
    // assert_equal(0, gnp(1, 1));
    // assert_equal(0, gnp(2, 1));
    // assert_equal(0, gnp(3, 1));
    // assert_equal(0, gnp(4, 1));
    // assert_equal(0, gnp(5, 1));
    // assert_equal(0, gnp(6, 1));
    // assert_equal(0, gnp(7, 1));
    // assert_equal(0, gnp(8, 1));
    // assert_equal(0, gnp(9, 1));
    // assert_equal(0, gnp(10, 1));
    // assert_equal(0, gnp(11, 1));
    // assert_equal(0, gnp(12, 1));
    // assert_equal(0, gnp(13, 3));
    // assert_equal(0, gnp2(13, 3));
    // assert_equal(0, gnp3(13, 3));
    // assert_equal(0, gnp(14, 1));
    // assert_equal(0, gnp(15, 1));
    assert_equal(0, gnp(16, 1));
}

int main() {
    test_gnp();
}